import React, {FC} from 'react';
import {Text, View} from "react-native";

const Payments:FC = () => {
    return (
        <View>
            <Text>Payments</Text>
        </View>
    );
};

export default Payments;